{
  "name": "catlair-BiliTools",
  "version": "0.0.1",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "prebuild": "rm -rf dist/",
    "predeploy": "cp -rf node_modules/ ./dist/node_modules/ && cp .env ./dist && rm -f ./dist/run.js",
    "build": "tsc && rm -rf ./dist/dto/",
    "deploy": "cp -f ./serverless/serverless.yaml ./dist && cd ./dist && sls deploy --debug",
    "dev": "ts-node ./src/run.ts",
    "start": "node ./dist/run.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/axios": "^0.14.0",
    "@types/node": "^14.14.10",
    "@types/qs": "^6.9.5"
  },
  "dependencies": {
    "axios": "^0.21.0",
    "dotenv": "^8.2.0",
    "qs": "^6.9.4"
  }
}
